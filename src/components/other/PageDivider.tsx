import React from 'react';

const PageDivider = () => {
    return (
        <div className="relative w-full my-8">
            <hr className="absolute inset-0 w-screen border-t border-gray-300"/>
        </div>
    );
};

export default PageDivider;
