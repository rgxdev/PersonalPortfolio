import {Inter, Lilita_One, Lobster, Permanent_Marker, Shadows_Into_Light} from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const lobster = Lobster({
    subsets: ['latin'],
    display: 'swap',
    weight: '400', // Add this line
});

export const shadowsIntoLight = Shadows_Into_Light({
    subsets: ['latin'],
    display: 'swap',
    weight: '400', // Add this line
});

export const lilitaOne = Lilita_One({
    subsets: ['latin'],
    display: 'swap',
    weight: '400', // Add this line
});

export const permanentMarker = Permanent_Marker({
    subsets: ['latin'],
    display: 'swap',
    weight: '400', // Add this line
});
